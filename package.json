{
  "name": "chess-clock",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "vue-tsc --noEmit && vite build && surge ~/chess-clock/dist goose-clock.surge.sh"
  },
  "dependencies": {
    "firebase": "^9.6.4",
    "nanotimer": "^0.3.15",
    "stopwatch-node": "^1.1.0",
    "ts-stopwatch": "^0.0.4",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@types/nanotimer": "^0.3.0",
    "@vitejs/plugin-vue": "^2.0.0",
    "typescript": "^4.4.4",
    "vite": "^2.7.2",
    "vue-tsc": "^0.29.8"
  }
}
